import React from 'react';
import ReactDOM from 'react-dom/client';
import TiptapEditor from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TiptapEditor />
  </React.StrictMode>
);
